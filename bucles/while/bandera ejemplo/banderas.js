//la bandera es un mecanismo de los ciclos. generalmente es una variable que cambia de estado,
 //haciendo el ciclo se ejecute o no


// un programa  que pida al usuario numeros. si alguno de estos numeros es 0, 
//el ciclo deja de ejecutarse.De lo contrario, el ciclo se ejecuta 

let 